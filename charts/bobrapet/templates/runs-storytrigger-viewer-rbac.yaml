apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "bobrapet.fullname" . }}-runs-storytrigger-viewer-role
  labels:
  {{- include "bobrapet.labels" . | nindent 4 }}
rules:
- apiGroups:
  - runs.bubustack.io
  resources:
  - storytriggers
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - runs.bubustack.io
  resources:
  - storytriggers/status
  verbs:
  - get
