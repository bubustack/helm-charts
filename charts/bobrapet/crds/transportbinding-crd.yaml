apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: transportbindings.transport.bubustack.io
spec:
  group: transport.bubustack.io
  names:
    categories:
    - bubu
    - transport
    kind: TransportBinding
    listKind: TransportBindingList
    plural: transportbindings
    shortNames:
    - tb
    - tbind
    singular: transportbinding
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.transportRef
      name: Transport
      type: string
    - jsonPath: .spec.stepName
      name: Step
      type: string
    - jsonPath: .spec.driver
      name: Driver
      type: string
    - jsonPath: .status.endpoint
      name: Endpoint
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TransportBinding is the Schema for the transportbindings API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of TransportBinding
            properties:
              audio:
                description: audio describes the desired audio flow for this binding.
                properties:
                  codecs:
                    description: |-
                      codecs enumerates the preferred codecs ordered by priority.
                      If empty, defaults to PCM16.
                    items:
                      description: AudioCodec describes a negotiated audio codec.
                      properties:
                        channels:
                          description: channels indicates the number of channels carried
                            by this codec.
                          format: int32
                          type: integer
                        name:
                          description: name of the codec (e.g., pcm16, opus, g711-mu)
                          enum:
                          - pcm16
                          - pcm24
                          - opus
                          - g711-mu
                          - g711-a
                          - g722
                          - aac
                          - mp3
                          - flac
                          - raw
                          type: string
                        sampleRateHz:
                          description: sampleRateHz indicates the sampling rate for
                            this codec.
                          format: int32
                          type: integer
                      required:
                      - name
                      type: object
                    type: array
                  direction:
                    description: direction indicates whether the owning engram publishes,
                      subscribes, or both.
                    enum:
                    - publish
                    - subscribe
                    - bidirectional
                    type: string
                required:
                - direction
                type: object
              binary:
                description: binary describes the desired binary flow for this binding.
                properties:
                  direction:
                    description: direction indicates whether the owning engram publishes,
                      subscribes, or both.
                    enum:
                    - publish
                    - subscribe
                    - bidirectional
                    type: string
                  mimeTypes:
                    description: mimeTypes enumerates supported MIME types (e.g.,
                      application/json, application/octet-stream).
                    items:
                      type: string
                    type: array
                required:
                - direction
                type: object
              connectorEndpoint:
                description: connectorEndpoint is the in-pod endpoint engrams should
                  dial to reach the connector sidecar (e.g., 127.0.0.1:50051).
                type: string
              driver:
                description: driver indicates which connector driver should be used.
                minLength: 1
                type: string
              engramName:
                description: engramName is the run-specific engram backing the step.
                minLength: 1
                type: string
              rawSettings:
                description: settings carries provider-specific configuration serialized
                  as JSON.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              stepName:
                description: stepName is the Story step that owns this binding.
                minLength: 1
                type: string
              storyRunRef:
                description: storyRunRef identifies the StoryRun this binding belongs
                  to when scoped to an individual run.
                properties:
                  name:
                    description: |-
                      Name of the referenced object.
                      Must be a valid DNS-1123 label as defined by Kubernetes naming conventions.
                    maxLength: 253
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                  namespace:
                    description: |-
                      Namespace of the referenced object.
                      If empty, defaults to same namespace as the referencing object.

                      Cross-namespace references should be used carefully and only when
                      multi-tenancy is properly configured. Consider the security implications:
                      - Ensure RBAC policies allow cross-namespace access
                      - Validate that the target namespace is trusted
                      - Monitor cross-namespace references for security compliance

                      Note: cross-namespace references are currently rejected by admission
                      validation in the operator.
                    maxLength: 63
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                  uid:
                    description: |-
                      UID of the referenced StoryRun for drift detection.
                      This field is populated by controllers and should not be set by users.

                      StoryRun drift detection helps maintain execution integrity and
                      enables proper cleanup of orphaned StepRuns.
                    type: string
                required:
                - name
                type: object
              transportRef:
                description: transportRef references the Transport CR driving this
                  binding.
                minLength: 1
                type: string
              video:
                description: video describes the desired video flow for this binding.
                properties:
                  codecs:
                    description: |-
                      codecs enumerates supported codecs. Encoded pass-through drivers should
                      declare the codecs they can forward.
                    items:
                      description: VideoCodec describes a negotiated video codec or
                        raw pixel format.
                      properties:
                        name:
                          description: name of the codec (e.g., h264, vp8, raw-yuv420)
                          enum:
                          - h264
                          - vp8
                          - vp9
                          - av1
                          - raw-yuv420
                          - raw-rgb
                          - raw-bgr
                          type: string
                        profile:
                          description: profile or format hint (e.g., baseline, high,
                            rgb)
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                  direction:
                    description: direction indicates whether the owning engram publishes,
                      subscribes, or both.
                    enum:
                    - publish
                    - subscribe
                    - bidirectional
                    type: string
                  raw:
                    description: |-
                      raw indicates whether raw frames are required for processing (e.g., CV tasks).
                      Drivers may use this hint to determine if decoding is necessary.
                    type: boolean
                required:
                - direction
                type: object
            required:
            - driver
            - engramName
            - stepName
            - transportRef
            type: object
          status:
            description: status defines the observed state of TransportBinding
            properties:
              conditions:
                description: conditions represent the current state of the TransportBinding
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              downstreamHost:
                description: |-
                  downstreamHost is the resolved downstream endpoint (host:port) for the engram owning this binding.
                  This is populated when the engram can accept direct P2P connections from upstream engrams.
                type: string
              endpoint:
                description: endpoint is the connector endpoint (host:port or unix
                  socket) for this binding.
                type: string
              negotiatedAudio:
                description: negotiatedAudio captures the codec the connector selected
                  for audio.
                properties:
                  channels:
                    description: channels indicates the number of channels carried
                      by this codec.
                    format: int32
                    type: integer
                  name:
                    description: name of the codec (e.g., pcm16, opus, g711-mu)
                    enum:
                    - pcm16
                    - pcm24
                    - opus
                    - g711-mu
                    - g711-a
                    - g722
                    - aac
                    - mp3
                    - flac
                    - raw
                    type: string
                  sampleRateHz:
                    description: sampleRateHz indicates the sampling rate for this
                      codec.
                    format: int32
                    type: integer
                required:
                - name
                type: object
              negotiatedBinary:
                description: negotiatedBinary captures the MIME type the connector
                  selected for binary payloads.
                type: string
              negotiatedVideo:
                description: negotiatedVideo captures the codec the connector selected
                  for video.
                properties:
                  name:
                    description: name of the codec (e.g., h264, vp8, raw-yuv420)
                    enum:
                    - h264
                    - vp8
                    - vp9
                    - av1
                    - raw-yuv420
                    - raw-rgb
                    - raw-bgr
                    type: string
                  profile:
                    description: profile or format hint (e.g., baseline, high, rgb)
                    type: string
                required:
                - name
                type: object
              observedGeneration:
                format: int64
                type: integer
              upstreamHost:
                description: |-
                  upstreamHost is the resolved upstream endpoint (host:port) that this engram should connect to.
                  This is populated when the connection can bypass the hub and use direct P2P routing.
                type: string
            type: object
        required:
        - spec
        type: object
    selectableFields:
    - jsonPath: .spec.transportRef
    - jsonPath: .spec.storyRunRef.name
    served: true
    storage: true
    subresources:
      status: {}

