apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "bobravoz-grpc.fullname" . }}-metrics-reader
  labels:
  {{- include "bobravoz-grpc.labels" . | nindent 4 }}
rules:
- nonResourceURLs:
  - /metrics
  verbs:
  - get
